<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pakgopay.mapper.CurrencyReportMapper">

    <!-- ResultMap (explicit javaType) -->
    <resultMap id="CurrencyReportResultMap" type="com.pakgopay.mapper.dto.CurrencyReportDto">

        <id column="currency" property="currency" javaType="java.lang.String"/>
        <id column="order_type" property="orderType" javaType="java.lang.Integer"/>
        <id column="record_date" property="recordDate" javaType="java.lang.Long"/>

        <result column="order_balance" property="orderBalance" javaType="java.math.BigDecimal"/>
        <result column="success_order_balance" property="successOrderBalance" javaType="java.math.BigDecimal"/>
        <result column="merchant_fee" property="merchantFee" javaType="java.math.BigDecimal"/>
        <result column="order_profit" property="orderProfit" javaType="java.math.BigDecimal"/>

        <result column="order_quantity" property="orderQuantity" javaType="java.lang.Long"/>
        <result column="success_quantity" property="successQuantity" javaType="java.lang.Long"/>

        <result column="create_time" property="createTime" javaType="java.lang.Long"/>
        <result column="update_time" property="updateTime" javaType="java.lang.Long"/>

        <result column="remark" property="remark" javaType="java.lang.String"/>

    </resultMap>

    <resultMap id="ReportCardSummaryResultMap" type="com.pakgopay.mapper.dto.ReportCardSummaryDto">
        <result column="total_number" property="totalNumber" javaType="java.lang.Long"/>
        <result column="total" property="total" javaType="java.math.BigDecimal"/>
        <result column="success_order_balance" property="successOrderBalance" javaType="java.math.BigDecimal"/>
    </resultMap>

    <!-- Insert -->
    <insert id="insert" parameterType="com.pakgopay.mapper.dto.CurrencyReportDto">
        INSERT INTO currency_report (
        <include refid="BaseColumns"/>
        ) VALUES (
        #{currency},
        #{orderType},
        #{orderBalance},
        #{successOrderBalance},
        #{merchantFee},
        #{orderProfit},
        #{orderQuantity},
        #{successQuantity},
        #{createTime},
        #{updateTime},
        #{recordDate},
        #{remark}
        )
    </insert>

    <!-- Base columns -->
    <sql id="BaseColumns">
        currency,
        order_type,
        order_balance,
        success_order_balance,
        merchant_fee,
        order_profit,
        order_quantity,
        success_quantity,
        create_time,
        update_time,
        record_date,
        remark
    </sql>

    <!-- Update by unique key -->
    <update id="update" parameterType="com.pakgopay.mapper.dto.CurrencyReportDto">
        UPDATE currency_report
        <set>
            <if test="orderBalance != null">order_balance = #{orderBalance},</if>
            <if test="successOrderBalance != null">success_order_balance = #{successOrderBalance},</if>
            <if test="merchantFee != null">merchant_fee = #{merchantFee},</if>
            <if test="orderProfit != null">order_profit = #{orderProfit},</if>
            <if test="orderQuantity != null">order_quantity = #{orderQuantity},</if>
            <if test="successQuantity != null">success_quantity = #{successQuantity},</if>

            <if test="remark != null">remark = #{remark},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE currency = #{currency}
        AND record_date = #{recordDate}
        AND order_type = #{orderType}
    </update>

    <insert id="batchUpsert" parameterType="java.util.List">
        INSERT INTO currency_report (
        <include refid="BaseColumns"/>
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.currency},
            #{item.orderType},
            #{item.orderBalance},
            #{item.successOrderBalance},
            #{item.merchantFee},
            #{item.orderProfit},
            #{item.orderQuantity},
            #{item.successQuantity},
            #{item.createTime},
            #{item.updateTime},
            #{item.recordDate},
            #{item.remark}
            )
        </foreach>
        ON CONFLICT (currency, record_date, order_type) DO UPDATE SET
        order_balance = EXCLUDED.order_balance,
        success_order_balance = EXCLUDED.success_order_balance,
        merchant_fee = EXCLUDED.merchant_fee,
        order_profit = EXCLUDED.order_profit,
        order_quantity = EXCLUDED.order_quantity,
        success_quantity = EXCLUDED.success_quantity,
        remark = EXCLUDED.remark,
        update_time = EXCLUDED.update_time
    </insert>

    <!-- Common WHERE conditions for BaseReportEntity -->
    <sql id="CurrencyReportWhere">
        <where>
            <if test="currency != null and currency != ''">
                AND currency = #{currency}
            </if>

            <if test="orderType != null">
                AND order_type = #{orderType}
            </if>

            <if test="startTime != null">
                AND record_date &gt;= #{startTime}
            </if>

            <if test="endTime != null">
                AND record_date &lt;= #{endTime}
            </if>
        </where>
    </sql>

    <!-- 1) Return card summary (count + total + success total) -->
    <select id="cardSummaryByQuery"
            parameterType="com.pakgopay.data.entity.report.BaseReportEntity"
            resultMap="ReportCardSummaryResultMap">
        SELECT
        COALESCE(COUNT(1), 0) AS total_number,
        COALESCE(SUM(order_balance), 0) AS total,
        COALESCE(SUM(success_order_balance), 0) AS success_order_balance
        FROM currency_report
        <include refid="CurrencyReportWhere"/>
    </select>

    <!-- 2) Page query -->
    <select id="pageByQuery"
            parameterType="com.pakgopay.data.entity.report.BaseReportEntity"
            resultMap="CurrencyReportResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM currency_report
        <include refid="CurrencyReportWhere"/>
        ORDER BY record_date DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>


</mapper>
