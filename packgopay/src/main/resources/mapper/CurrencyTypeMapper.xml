<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pakgopay.mapper.CurrencyTypeMapper">
    <resultMap id="currencyTypeMap" type="com.pakgopay.mapper.dto.CurrencyTypeDTO">
        <id column="id" property="id" javaType="Integer"/>
        <result column="currency_type" property="currencyType" javaType="String"/>
        <result column="name" property="name" javaType="String"/>
        <result column="icon" property="icon" javaType="String"/>
        <result column="create_time" property="createTime" javaType="java.lang.Long"/>
        <result column="update_time" property="updateTime" javaType="java.lang.Long"/>
        <result column="create_by" property="createBy" javaType="String"/>
        <result column="update_by" property="updateBy" javaType="String"/>
        <result column="remark" property="remark" javaType="String" />
        <result column="currency_scale" property="currencyAccuracy" javaType="java.math.BigDecimal"/>
        <result column="timezone" property="timezone" javaType="String"/>
    </resultMap>
    <sql id="allColumn">
        id, currency_type, name, icon, create_time, create_by, update_time, update_by, remark, currency_scale, timezone
    </sql>
    <sql id="insertColumn">
        currency_type, name, icon, create_time, create_by, update_time, update_by, remark, currency_scale, timezone
    </sql>
    <select id="getAllCurrencyType" parameterType="com.pakgopay.data.reqeust.currencyTypeManagement.CurrencyTypeRequest" resultMap="currencyTypeMap">
        SELECT
        <include refid="allColumn"/>
        FROM currency
        WHERE 1 = 1
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="name != null">
            and name = #{name}
        </if>
        <if test="!allData">
            LIMIT #{pageSize} OFFSET #{offSet}
        </if>

    </select>

    <insert id="addNewCurrency">
        INSERT INTO currency (
                              <include refid="insertColumn"/>
        ) values (#{currencyType},#{name},#{icon},#{createTime},#{createBy},#{updateTime},#{updateBy},#{remark},#{currencyAccuracy},#{timezone})
    </insert>

    <update id="updateCurrencyType">
        UPDATE currency
        <set>
            <if test="currencyAccuracy != null">currency_scale = #{currencyAccuracy},</if>
            <if test="timezone != null and timezone != ''">timezone = #{timezone},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="getCurrencyById" resultMap="currencyTypeMap">
        SELECT
          <include refid="allColumn"/>
            FROM currency WHERE id = #{id}
    </select>

    <select id="getCount" resultType="java.lang.Integer"
            parameterType="com.pakgopay.data.reqeust.currencyTypeManagement.CurrencyTypeRequest"
    >
        SELECT count(1) from currency where 1 = 1
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="name != null">
            and name = #{name}
        </if>

    </select>

    <select id="getTimezoneByCurrencyType" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT timezone
        FROM currency
        WHERE currency_type = #{currencyType}
        LIMIT 1
    </select>

    <select id="getCurrencyByCurrencyType" parameterType="java.lang.String" resultMap="currencyTypeMap">
        SELECT
        <include refid="allColumn"/>
        FROM currency
        WHERE currency_type = #{currencyType}
        LIMIT 1
    </select>

</mapper>
